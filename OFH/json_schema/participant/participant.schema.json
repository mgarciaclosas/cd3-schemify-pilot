{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.example.org/cd3-ofh-pilot/participant/participant.schema.json",
  "title": "Our Future Health — Participant data",
  "description": "Array of Our Future Health participant records. One element is one participant: registration and consent metadata, and demographics data.",
  "$comment": "Standard JSON Schema cannot enforce that PID is unique across rows, and this table's uniqueItems cannot anchor to a checked key format (D025). See the README's 'Documented but not enforced' section once written.",
  "type": "array",
  "minItems": 1,
  "uniqueItems": true,
  "items": {
    "type": "object",
    "title": "Participant data record",
    "allOf": [
      {
        "$ref": "categories/participant.json"
      }
    ],
    "unevaluatedProperties": false
  }
}
