# Sources — CD3 pilot: BGS (Breast Cancer Now Generations Study) derived variables

## Dictionary files

- `DerivedVariables_Schema.json` (copied to `CD3_Schemify_Pilot/BGS/raw_data/`, originally `Schema_and_Derivation_Utils/Questionnaire/R0/schemas/derived/DerivedVariables_Schema.json`) · JSON Schema, draft 2020-12 · 60 properties (`TCode` + 59 `R0_*` derived variables) describing one participant's R0 (baseline) analysis-ready derived variables. Each property carries `description`, most carry `x-description` (derivation logic in prose), `x-derivedFrom` (pointers into sibling pseudo-anonymised schemas, or for the 10 `R0_FamHist*` properties, legacy SAS script paths under `N:/BrBreakthrough/...`), `x-calculation` (formula, where applicable), `x-formerName` (legacy variable name). The file's own `$defs` are unresolved `$ref`s to sibling schemas (`AdminEvents`, `GeneralInformation`, `PhysicalDevelopment`, `Pregnancies`, `ContraceptiveHRT`, `BreastDisease`, `CancerRelatives`, `MH_Illnesses`, `AlcoholSmokingDiet`, `MenstrualMenopause`, `PhysicalActivity`) that are not present in this package — informational only (they document lineage), not resolvable or needed for conversion. Missingness in the source is inconsistent: most fields type-permit bare `null`; a `9999` "Not Applicable" const branch appears on 13 fields where a derivation can be structurally inapplicable (e.g. too young, never pregnant); no field carries an explicit source-documented generic-missing code. Registered 2026-09-15. **Routing scan (2026-09-21):** signals seen. (a) `9999` "Not Applicable" branch on 13 fields (R0_PregAt20, R0_Height20, R0_Weight20, R0_BMI20, R0_BMI, R0_WaistHipRatio, R0_AgeStartedOC, R0_AgeLastUsedOC, R0_OCLength, R0_AgeFFTP, R0_AgeLFTP, R0_BreastfeedingDuration, R0_Breastfed). (b) Stated rules in `x-description`: BMI20 / BMI / WaistHipRatio set to 9999 by pregnancy; AgeFFTP / AgeLFTP / BreastfeedingDuration / Breastfed 9999 when no live birth; AgeDiabetes retained only if DiabetesStatus=1; AgeMenarche null if EverHadPeriods=2. Five register rows in `ROUTING.csv`. (c) Unstated gate-and-detail pairs (status + ages) for OC, HRT, alcohol, smoking, menopause, diabetes; the 9999 branch on Height20/Weight20/OC ages has no stated trigger. None of these registered until the routing policy is set.

## External sources

- Generations Study schema repository · https://github.com/UK-Generations-Study/Schema_and_Derivation_Utils · would resolve: sibling pseudo-anonymised schemas, derivation scripts, legacy SAS scripts referenced by the R0_FamHist* variables · steward-approved 2026-09-21 (D006); consulted 2026-09-21: `Documentation/JSON Schema Conventions.md` (null = "Missing or invalid", 9999 = not applicable, no skip-logic guidance) and folder listings; `Questionnaire/R0/scripts/Derivation.ipynb` consulted 2026-09-21, raw file read (D028, D037; R0_CurrentPreg coding checked in the legacy Pregnancies_Schema_PseudoAnon.json); `Questionnaire/R0/README.pdf`, `R0_workflow.pdf`, `schemas/` not yet read

## The steward

- the steward · first-hand: all of it, answers questions on this package alone (D007) · defers to: nobody named
- ask here: interpretation of the source, sentinel and category choices, routing · not here: nothing so far
